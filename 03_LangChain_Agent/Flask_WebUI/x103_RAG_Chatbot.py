"""
LangChain RAG Agent 기반 Chatbot (Flask + HTML)

- 에이전트 구성은 103_RAG_Agent 노트북 내용을 반영:
  - 나이키 10-K PDF를 인덱싱(로드 -> 분할 -> 임베딩 -> 벡터 스토어 저장)하고,
    벡터 스토어 검색을 도구(retrieve_context)로 감싼 에이전트 기반 RAG(Agentic RAG)
  - InMemorySaver(checkpointer) 기반 단기 메모리로 대화 이력을 자동 관리
- 실행: python x103_RAG_Chatbot.py  ->  브라우저에서 http://localhost:8000 접속

라우트 구성:
  GET  /         채팅 UI(103_RAG_Chatbot.html) 반환
  POST /chat     {"message": "..."} 를 받아 에이전트 실행 후 {"reply": "..."} 반환
  POST /reset    대화 기록 초기화 (새 thread_id 발급)
  POST /summary  지금까지의 대화 내용을 LLM으로 요약하여 반환
"""

# ---------------------------------------------------------------------------------
# import (표준 라이브러리 / Flask / LangChain / LangGraph)
# ---------------------------------------------------------------------------------












# ---------------------------------------------------------------------------------
# 환경 변수 로드 및 기본 상수 정의 (MODEL_NAME, PORT, BASE_DIR, PDF_PATH, SYSTEM_PROMPT)
# - PDF_PATH: BASE_DIR / "example_data" / "nke-10k-2023_korean.pdf"
# - SYSTEM_PROMPT: retrieve_context 도구로 먼저 검색한 후 검색된 정보를 바탕으로 답변,
#   검색 결과에 없는 내용은 모른다고 답하도록 지시
# ---------------------------------------------------------------------------------







# ---------------------------------------------------------------------------------
# 인덱싱: RAG Agent를 위한 벡터 스토어 구축 (103_RAG_Agent 노트북 1장과 동일)
# - 서버 시작 시 1회만 수행: PDF 로드 -> 분할 -> 임베딩 -> 벡터 스토어 저장
# ---------------------------------------------------------------------------------

# 1) PDF 로드 (PyPDFLoader)



# 2) 텍스트 분할 (RecursiveCharacterTextSplitter: chunk_size=1000, chunk_overlap=200)





# 3) 임베딩 + 벡터 스토어 저장 (OpenAIEmbeddings + InMemoryVectorStore + add_documents)





# 인덱싱 결과 출력 (페이지 수, 청크 수)



# ---------------------------------------------------------------------------------
# 검색 도구 정의 (103_RAG_Agent 노트북 2장과 동일)
# ---------------------------------------------------------------------------------

# retrieve_context: 벡터 스토어에서 질문과 유사한 문서를 검색하는 도구
#   - @tool(response_format="content_and_artifact") 사용
#     -> LLM에게 전달할 문자열(content)과 원본 문서(artifact)를 함께 반환
#   - similarity_search(query, k=2)로 검색
#   - 검색된 문서를 "출처: ... (페이지 ...)\n내용: ..." 형식의 문자열로 직렬화
#   - (직렬화된 문자열, 문서 리스트) 튜플 반환















# ---------------------------------------------------------------------------------
# LLM 및 RAG Agent 초기화 (서버 시작 시 1회만 생성해서 재사용)
# - InMemorySaver를 checkpointer로 사용
#   -> 같은 thread_id로 호출하면 이전 대화를 자동으로 기억 (단기 메모리)
#   -> 서버 프로세스가 종료되면 대화 이력도 사라짐
# ---------------------------------------------------------------------------------

# llm 생성 (init_chat_model)

# checkpointer 생성 (InMemorySaver)

# agent 생성 (create_agent: llm + [retrieve_context] + system_prompt + checkpointer)





# ---------------------------------------------------------------------------------
# 대화 스레드 관리
# - 대화 이력 자체는 checkpointer가 thread_id별로 저장하므로,
#   서버는 "현재 사용 중인 thread_id"만 관리하면 됨
# - 대화 초기화는 새 thread_id를 발급하는 방식으로 구현 (빈 대화에서 새로 시작)
# - threaded=True로 실행하면 요청이 동시에 처리될 수 있으므로 Lock으로 보호
# ---------------------------------------------------------------------------------




# current_config(): 현재 thread_id로 에이전트 호출용 config 생성
#   반환 형식: {"configurable": {"thread_id": "conversation_N"}}





# ---------------------------------------------------------------------------------
# Flask 앱과 라우트
# ---------------------------------------------------------------------------------



# GET / : 채팅 UI 페이지(103_RAG_Chatbot.html) 반환 (send_file 사용)





# POST /chat : 사용자 메시지를 받아 에이전트를 실행하고 답변을 JSON으로 반환
#   - request.get_json()에서 "message" 추출, 비어 있으면 400 에러
#   - 이전 대화 이력은 checkpointer가 thread_id 기준으로 자동 관리하므로
#     매 요청마다 새 사용자 메시지 하나만 전달하면 됨
#   - agent.invoke() 결과의 마지막 메시지에서 답변 추출













# POST /reset : 대화 기록 초기화 - 새 thread_id를 발급하여 빈 대화에서 새로 시작






# POST /summary : checkpointer에 저장된 현재 스레드의 대화 이력을 LLM으로 요약하여 반환

    # 저장된 대화 이력 조회 (agent.get_state() 사용)



    # 대화가 없으면 "요약할 대화가 없습니다." 반환



    # 메시지 역할별로 텍스트로 변환 (System / User / AI, 도구 호출 등 그 외 메시지는 제외)










    # 요약 프롬프트 생성 후 LLM에게 요약 요청 (llm.invoke)







# ---------------------------------------------------------------------------------
# 서버 실행 (host="127.0.0.1", port=PORT, threaded=True)
# ---------------------------------------------------------------------------------
